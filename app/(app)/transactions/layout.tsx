import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Riwayat Transaksi — Kas Kawan",
};

export default function TransactionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
