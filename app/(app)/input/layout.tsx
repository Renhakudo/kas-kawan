import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Catat Transaksi — Kas Kawan",
};

export default function InputLayout({ children }: { children: React.ReactNode }) {
  return children;
}
